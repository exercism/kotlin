# Hints

## 1. Generate an id number

- The function should return another function.
- The returned function needs to access the `day` parameter.

## 2. Protect the recovered secret plans with a secret password

- The returned function has only one parameter.
- Strings can be compared using [equality].

## 3. Find the secret password

- Call the minion's function with another function that performs our part of the algorithm.

[equality]: https://kotlinlang.org/docs/equality.html#structural-equality